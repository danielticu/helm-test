apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-app-helm
  namespace: default
spec:
  project: default

  source:
    repoURL: https://github.com/danielticu/helm-test.git
    path: test-app-helm
    targetRevision: HEAD
    helm:
      releaseName: test-app-helm

  destination:
    server: https://kubernetes.default.svc
    namespace: test-app-helm

  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true