appName: app2
environment: test
namespace: athena

image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1.27.4"

replicaCount: 5
