apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-argocd-apps.fullname" . }}
  labels:
    {{- include "my-argocd-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-argocd-apps.selectorLabels" . | nindent 4 }}
