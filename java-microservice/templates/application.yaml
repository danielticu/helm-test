apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.appName }}
  namespace: default
spec:
  project: default
  source:
    repoURL: https://github.com/danielticu/helm-test.git
    targetRevision: HEAD
    path: java-microservice
    helm:
      valueFiles:
        - "../values/default-values.yaml"
        - "../values/environment/environment-values.yaml"
        - "../values/environment/namespace/namespace-values.yaml"
        - "../values/environment/namespace/{{ .Values.appName }}-values.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
